# 多线程编程

## 1、进程和线程概念    

进程是操作系统进行资源分配的基本单位，可以理解为一个正在运行的程序；类似一个公司

线程是进程中的执行单元，一个进程可以包含多个线程。类似公司里的员工

不同进程通常拥有独立的内存空间，而同一进程中的线程共享大部分资源，因此线程之间通信方便，但也需要考虑共享数据的线程安全问题。

| 对比     | 进程                   | 线程                                 |
| -------- | ---------------------- | ------------------------------------ |
| 含义     | 正在运行的程序         | 进程里的执行单元                     |
| 内存     | 不同进程通常独立       | 同一进程的线程共享内存               |
| 创建开销 | 较大                   | 较小                                 |
| 通信     | 相对复杂               | 共享数据方便                         |
| 风险     | 一个进程崩溃通常较独立 | 一个线程严重异常可能导致整个进程崩溃 |

## 2、 创建线程     

所有事情都放在主线程，那么某一步耗时，界面就可能卡住。

创建线程，把任务拆开：

```
c++：
#include <thread>

void task()
{
    // 要执行的任务
}

int main()
{
    std::thread t(task);

    t.join();

    return 0;
}

qt里面：

QThread *thread = new QThread;
worker->moveToThread(thread);
thread->start();
```

创建线程就是让程序开启新的执行任务，使多个任务能够并发运行。C++中可以使用 `std::thread` 创建线程；Qt项目中也常使用 `QThread`

## 3、 线程同步     

多个线程一起工作时，要**协调它们访问共享数据的顺序**，避免互相干扰。

如果两个线程同时操作同一个普通队列，就可能出现数据竞争，所以要做线程同步。

最常见的方法有：

- **互斥锁**：同一时间只允许一个线程访问共享资源；解决谁先访问共享资源，保护共享资源
- **条件变量**：没数据时让线程等待，有数据时再唤醒；解决什么时候该继续执行，实现线程等待和唤醒
- **信号量**：通过计数控制可用资源数量。解决同时允许几个线程使用资源，比如某个资源最多允许3个线程同时使用，信号量=3，A拿走一个，B拿走一个,C拿走一个，D再来就没有通行证了，只能等待。等线程A结束，则信号量=1，线程D可以继续。
- **临界区**：Windows 下常见的互斥同步方式。这个也是保护共享资源

## 4、 互斥锁    

**给共享资源上一把锁，同一时间只允许一个线程进去操作。**

```
线程A想访问队列
↓
先加锁
↓
操作队列
↓
解锁
```

这时候线程B如果也想访问：

```
线程B
↓
发现锁被占用
↓
等待
↓
线程A解锁
↓
线程B获得锁
↓
再操作队列
```

```
std::mutex mtx;

mtx.lock();

// 操作共享数据

mtx.unlock();
```

或

```
std::lock_guard<std::mutex> lock(mtx);

// 操作共享数据
```

## 5、 条件变量    
条件变量一般不是单独使用，而是和互斥锁一起用。互斥锁负责保护队列，条件变量负责等待和唤醒。

让线程在“条件不满足”时先睡觉，等条件满足了再把它叫醒。

```
处理线程发现队列为空
   ↓
进入等待状态
   ↓
不再一直占用CPU

采集线程放入新图像
   ↓
发出通知
   ↓
唤醒处理线程
   ↓
处理线程开始取图
```

条件变量的作用不是“保护数据”，而是：

> **协调线程什么时候该等待、什么时候该继续执行。**
>
> > 互斥锁解决“谁能访问”；
> >  条件变量解决“什么时候访问”。

## 6、 死锁     

多个线程因为互相等待对方持有的资源，导致所有线程都无法继续执行

多个线程同时持有多把锁，并且加锁顺序不一致。避免死锁的方法包括统一加锁顺序、缩小锁的作用范围、避免长时间持锁，以及尽量不要在持锁期间执行耗时操作。

同一组锁，所有线程都按同样顺序申请。

**4个必要条件**。只有这四个条件同时满足，才可能发生死锁。

```
互斥条件：某个资源同一时间只能被一个线程占用。比如一把互斥锁被线程A拿到后，线程B只能等。

占有且等待：线程已经占有了一部分资源，同时还在等待其他资源。比如线程A已经拿到锁1，还在等锁2。

不可剥夺条件：已经分配给线程的资源，不能被系统强制抢走，只能由线程自己释放。

循环等待条件：多个线程形成环形等待。比如：线程A 等 线程B 的锁    线程B 等 线程C 的锁    线程C 又等 线程A 的锁
```



## 7、线程安全

指多个线程并发访问共享资源时，无论线程执行顺序如何，程序都能保持数据一致性和正确性。

线程安全是最终目标，线程同步是实现这个目标的一种手段。比如互斥锁、条件变量、信号量这些同步机制，都是为了保证多线程访问共享资源时是安全的。
