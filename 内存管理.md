# 内存管理

## 1、指针

为什么用指针：不想复制整个对象，而是传递对象地址。减少开销。

指针就是一个专门用来保存内存地址的变量。

```
int a = 10;
int *p = &a;
```

这里：

- `a` 里面存的是 `10`
- `&a` 表示 `a` 的内存地址， `&`：取地址
- `p` 保存了这个地址，p是指针变量
- `*p` 表示通过这个地址访问 `a` 的值，根据 `p` 保存的地址，访问这个地址里面的数据。

指针经常用于动态内存管理、对象传递以及底层数据访问。

```
delete p;  把p当前指向的那块动态内存释放掉。
例如：delete之后，p 里面可能仍然是 0x1000，但是 0x1000 那块内存已经不再属于你
```

## 2、空指针

**没有指向任何有效对象**的指针。

```
int *p = nullptr;//p 当前没有指向任何有效对象。
若是直接*p=10,可能导致程序崩溃。所以在项目里面要判断if(p)先确认指针有效，再访问。
```

## 3、野指针/悬空指针

指针还保存着一个地址，但是那个地址对应的对象已经不存在了。

野指针通常是指**指向无效内存的指针**，例如**对象已经释放，但指针仍然保存原地址**。如果继续访问，可能造成程序崩溃或者数据异常。

空指针比较容易判断；野指针更危险，因为它表面上可能不是空，但对应资源已经失效。

## 4、引用

指针保存的是对象地址，可以为空，也可以改变指向

引用是对象的别名，定义后通常必须绑定一个对象，并且不能再改为引用另一个对象。

引用传递？

## 5、new和delete

new表示在堆上申请一块内存，并返回地址给 `p`。

```
int *p = new int;
```

使用完：

```
delete p;
```

释放。



## 6、内存泄漏

内存泄漏是程序**申请了动态内存**，但使用**完成后没有正确释放**，并且**后续又无法继续管理这块内存，导致内存一直被占用。**

**向系统借了内存（`new`），但一直没还（`delete`），这块内存就永远被占着，别人也用不了 —— 这就是内存泄漏。**

## 7、智能指针

智能指针是**帮助程序自动管理内存的工具**，可以自动记录对象有没有使用，不会被使用的时候就自动释放内存，避免忘记释放（导致内存泄漏)或提前释放(还需要去使用造成野指针)

## 8、二级指针

普通指针保存变量的地址，二级指针保存“指针变量本身的地址。

```
int **pp;

a = 10
p → a
pp → p
```

## 9、对象生命周期

一个对象从“被创建”到“被销毁”的整个过程。

```
创建对象   ↓ 调用构造函数   ↓ 对象被使用   ↓ 对象不再需要   ↓ 调用析构函数   ↓ 对象销毁
```

若是动态创建的对象，例如：

```
Camera *p = new Camera;
```

对象不会因为当前函数结束就自动销毁，而是一直存在，直到delete p; 

```
new ↓ 对象创建 ↓ 一直存在 ↓ delete ↓ 对象销毁
```

如果对象已经销毁了，你还继续访问它，就可能产生野指针；如果对象该销毁却一直没销毁，就可能产生内存泄漏。